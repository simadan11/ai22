# Зависимости для Vercel Python Functions.
# Стандартная библиотека Python покрывает всё, что нужно этому мини-API.
# Если будешь подключать ИИ/БД — раскомментируй нужное:
#
# openai>=1.0.0          # OpenAI API
# anthropic>=0.20.0      # Claude API
# requests>=2.31.0       # HTTP-вызовы к локальному ПК / webhook'ам
# upstash-redis>=1.0.0   # Vercel KV (постоянное состояние между холодными стартами)
